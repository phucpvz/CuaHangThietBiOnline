<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.phuphuc.cuahangthietbionline">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_vsmart"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_vsmart_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CửaHàngThiếtBịOnline"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.LienHeActivity"></activity>
        <activity android:name=".activity.ThongTinActivity" />
        <activity android:name=".activity.LaptopActivity" />
        <activity android:name=".activity.DienThoaiActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>